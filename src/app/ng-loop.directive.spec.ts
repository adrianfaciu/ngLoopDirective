import { NgLoopDirective } from './ng-loop.directive';

describe('NgLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new NgLoopDirective();
    expect(directive).toBeTruthy();
  });
});
